node_modules
dist
.next
coverage
pnpm-lock.yaml

# Mutation testing output.
.stryker-tmp
reports

# Vendored agent-skill templates. They are documentation for a tool, not code
# this repo ships, and reformatting someone else's template teaches us nothing
# about our own. ESLint already ignores these — keeping the two in step means
# `pnpm lint` and `pnpm format:check` agree on what counts as our code.
.claude
.agents

# Generated by Next on every build — formatting it just creates churn.
next-env.d.ts

# Prose is hand-wrapped for readability; Prettier's reflow fights that.
*.md
